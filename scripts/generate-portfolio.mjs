import fs from'node:fs/promises';import path from'node:path';import{fileURLToPath}from'node:url';
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..'),out=path.join(root,'generated-portfolio');
const cfg=JSON.parse(await fs.readFile(path.join(root,'portfolio.config.json'),'utf8')),domains=JSON.parse(await fs.readFile(path.join(root,'domains.config.json'),'utf8')),domainAssets=JSON.parse(await fs.readFile(path.join(root,'domain-assets.config.json'),'utf8'));
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));
const pad=n=>String(n).padStart(2,'0'),siteDir=v=>`${pad(v.rank)}-${v.slug}`,topicUrl=(v,i)=>`/${siteDir(v)}/guides/${pad(i+1)}/`;
const registeredDomains=new Set(domainAssets.domains.map(x=>x.domain));
const aliasesFor=slug=>domainAssets.domains.filter(x=>x.vertical===slug&&x.role==='redirect').map(x=>x.domain);
const assetCost=domainAssets.domains.reduce((sum,x)=>sum+domainAssets.pricingProfiles[x.profile].firstYearTotal,0);
const riskLabel={standard:'通常レビュー',review:'重点レビュー',ymyl:'専門家レビュー必須',sensitive:'安全・プライバシーレビュー必須',adult:'18歳確認・法務レビュー必須'};
function shell(v,title,desc,body,{canonical='/',adult=false}={}){return`<!doctype html><html lang="ja"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="robots" content="noindex,follow"><title>${esc(title)} | ${esc(v.brand)}</title><meta name="description" content="${esc(desc)}">${adult?'<meta name="rating" content="adult"><meta name="rating" content="RTA-5042-1996-1400-1577-RTA">':''}<link rel="canonical" href="https://${domains[v.slug]}${canonical}"><link rel="stylesheet" href="/assets/portfolio.css"></head><body${adult?' data-adult="true"':''}><header><a href="/${siteDir(v)}/"><span>${esc(v.brand.slice(0,1))}</span><b>${esc(v.brand)}</b></a><nav><a href="/${siteDir(v)}/">ホーム</a><a href="/${siteDir(v)}/#guides">ガイド</a><a href="/">19サイト一覧</a></nav></header>${body}<footer><b>${esc(v.brand)}</b><p>公開前のMVPドラフトです。事実確認・独自検証・広告設定後にindexを許可してください。</p><small>Domain candidate: ${esc(domains[v.slug])}</small></footer>${adult?ageGate():''}</body></html>`}
function ageGate(){return`<dialog id="ageGate"><form method="dialog"><strong>18+</strong><h2>成人向けサイトです</h2><p>18歳未満の方は閲覧できません。</p><div><a href="/">退出</a><button id="ageOk">18歳以上です</button></div></form></dialog><script>const g=document.querySelector('#ageGate');if(sessionStorage.getItem('portfolioAdult')!=='yes')g.showModal();document.querySelector('#ageOk').onclick=()=>sessionStorage.setItem('portfolioAdult','yes')</script>`}
function home(v){const guides=v.topics.map((t,i)=>`<a class="guide-card" href="${topicUrl(v,i)}"><small>GUIDE ${pad(i+1)}</small><h2>${esc(t)}の選び方・比較・検証</h2><p>条件、比較軸、一次情報、注意点、収益導線を一枚の設計にまとめます。</p><span>設計を見る →</span></a>`).join('');const body=`<main><section class="hero"><div><p class="eyebrow">RANK ${pad(v.rank)} / 19</p><h1>${esc(v.name)}を<br>判断できる情報へ。</h1><p>${esc(v.reason)}</p><div class="domain">DOMAIN CANDIDATE <b>${esc(domains[v.slug])}</b></div></div><aside><span>MONETIZATION</span>${v.revenue.map((x,i)=>`<p><b>${pad(i+1)}</b>${esc(x)}</p>`).join('')}<small>${esc(riskLabel[v.risk])}</small></aside></section><section class="strategy"><div><p class="eyebrow">SITE STRATEGY</p><h2>${v.topics.length}サブジャンルを専門ページ化</h2></div><p>各ページは公開前状態です。実測・取材・公式情報を追加し、人間レビューを通したページだけindex対象へ変更します。</p></section><section id="guides" class="guides">${guides}</section></main>`;return shell(v,`${v.name}専門サイトMVP`,v.reason,body,{adult:v.adult})}
function guide(v,topic,i){const questions=questionSet(v,topic),schema={"@context":"https://schema.org","@type":"WebPage",name:`${topic}の選び方・比較・検証`,isFamilyFriendly:!v.adult,about:topic};const body=`<main class="article"><nav><a href="/${siteDir(v)}/">${esc(v.name)}</a><span>/</span><span>${esc(topic)}</span></nav><header class="article-title"><p class="eyebrow">CONTENT BLUEPRINT / ${pad(i+1)}</p><h1>${esc(topic)}の選び方・比較・検証ガイド</h1><p>${esc(topic)}について、広告順位ではなく、条件・根拠・向く人・注意点から判断できるページを作るための公開前設計です。</p><div><span>DRAFT</span><span>NOINDEX</span><span>${esc(riskLabel[v.risk])}</span></div></header><article><aside class="direct"><b>直接回答の設計</b><p>${esc(topic)}は、価格だけでなく「利用条件」「総費用」「解約・保証」「運営者」「実測または一次体験」を確認して選びます。最終原稿では対象読者と検証結果を具体的に記載します。</p></aside><section><h2>比較する項目</h2><table><thead><tr><th>比較軸</th><th>確認する情報</th><th>一次情報の作り方</th></tr></thead><tbody>${questions.map(q=>`<tr><td>${esc(q.axis)}</td><td>${esc(q.check)}</td><td>${esc(q.evidence)}</td></tr>`).join('')}</tbody></table></section><section><h2>作成するコンテンツ</h2><div class="formats"><div><b>01</b><h3>選び方</h3><p>初心者が判断を誤りやすい条件と例外を説明。</p></div><div><b>02</b><h3>比較・ランキング</h3><p>広告報酬から独立した評価表と根拠を掲載。</p></div><div><b>03</b><h3>実体験・検証</h3><p>検証条件、期間、結果、限界を公開。</p></div><div><b>04</b><h3>FAQ・更新</h3><p>追加質問と変更履歴を継続更新。</p></div></div></section><section><h2>収益導線</h2><div class="revenue">${v.revenue.map(x=>`<div><span>PR枠</span><b>${esc(x)}</b><p>本文の判断材料を提示した後に、明示的な広告枠として設置します。</p><button disabled>提携設定後に有効化</button></div>`).join('')}</div></section><section class="review"><h2>公開前チェック</h2>${cfg.defaults.requiredReview.map(x=>`<label><input type="checkbox" disabled> ${esc(x)}</label>`).join('')}<p>全項目の確認と、独自情報の追加が終わるまで noindex を維持します。</p></section></article><script type="application/ld+json">${JSON.stringify(schema)}</script></main>`;return shell(v,`${topic}の選び方・比較・検証`,`${topic}の比較条件、一次情報、注意点を整理する専門ページ。`,body,{canonical:`/guides/${pad(i+1)}/`,adult:v.adult})}
function questionSet(v,t){const base=[{axis:'料金・総費用',check:'初期費用、継続費用、追加費用',evidence:'同じ利用条件で総額を計算'},{axis:'品質・性能',check:'仕様値と実使用の差',evidence:'条件を固定して複数回検証'},{axis:'契約・保証',check:'解約、返品、保証、問い合わせ',evidence:'公式規約と実際の手順を確認'},{axis:'向く人・向かない人',check:'利用目的、頻度、環境による違い',evidence:'複数の利用シナリオで比較'}];if(v.risk==='ymyl')base.push({axis:'専門性・安全性',check:'資格者、根拠、禁忌、個人差',evidence:'専門家レビューと公的資料'});if(v.risk==='sensitive')base.push({axis:'安全・プライバシー',check:'本人確認、通報、データ管理',evidence:'規約・窓口・退会手順を確認'});if(v.adult)base.push({axis:'成人・同意確認',check:'年齢、同意、削除申請、決済表示',evidence:'運営方針と申請窓口を確認'});return base}
const css=`:root{--ink:#14231f;--paper:#f4f1e9;--accent:#e34b35;--line:#dcded7}*{box-sizing:border-box}html{font-family:Arial,'Noto Sans JP',sans-serif;background:var(--paper);color:var(--ink)}body{margin:0}body>header{height:72px;padding:0 max(20px,calc((100vw - 1120px)/2));display:flex;align-items:center;border-bottom:1px solid var(--line)}header>a{display:flex;align-items:center;gap:9px;color:var(--ink);text-decoration:none}header>a span{display:grid;place-items:center;width:32px;height:32px;border-radius:50%;background:var(--accent);color:#fff;font:700 18px Georgia}header nav{margin-left:auto;display:flex;gap:25px}header nav a{font-size:11px;color:var(--ink)}main{max-width:1120px;margin:auto}.hero{min-height:500px;display:grid;grid-template-columns:1.5fr .7fr;align-items:center;gap:80px}.eyebrow{color:var(--accent);font-size:9px;font-weight:800;letter-spacing:.15em}.hero h1{font-size:54px;line-height:1.2;letter-spacing:-.055em;margin:15px 0}.hero>div>p:not(.eyebrow){color:#68736f;line-height:1.9;max-width:610px}.domain{margin-top:25px;font-size:9px}.domain b{display:block;font-size:17px;margin-top:5px}.hero aside{background:var(--ink);color:#fff;padding:28px}.hero aside>span{font-size:8px;color:#94a49f}.hero aside p{display:flex;gap:12px;font-size:11px;border-top:1px solid #30413c;padding-top:12px}.hero aside p b{color:var(--accent)}.hero aside small{display:inline-block;background:var(--accent);padding:5px 8px;font-size:8px}.strategy{border-top:1px solid var(--line);display:grid;grid-template-columns:1fr 1fr;padding:55px 0}.strategy h2{font-size:28px}.strategy>p{color:#68736f;font-size:12px;line-height:1.8}.guides{display:grid;grid-template-columns:repeat(3,1fr);gap:14px}.guide-card{background:#fff;border:1px solid var(--line);padding:22px;color:var(--ink);text-decoration:none}.guide-card small{font-size:8px;color:var(--accent)}.guide-card h2{font-size:17px;line-height:1.5}.guide-card p{font-size:10px;line-height:1.7;color:#68736f}.guide-card span{font-size:9px;font-weight:800}.article{max-width:920px}.article>nav{display:flex;gap:8px;padding:25px 0;font-size:9px}.article>nav a{color:#68736f}.article-title{padding:55px 0}.article-title h1{font-size:42px;line-height:1.35}.article-title>p:not(.eyebrow){color:#68736f;line-height:1.8}.article-title>div span{font-size:8px;background:#6b7471;color:#fff;padding:5px 7px;margin-right:5px}.article-title>div span:first-child{background:var(--accent)}.direct{background:#fff;border-left:4px solid var(--accent);padding:24px}.direct b{font-size:10px;color:var(--accent)}.direct p{font-size:15px;line-height:1.9}.article article>section{margin-top:55px}.article h2{font-size:25px}table{width:100%;border-collapse:collapse;font-size:11px;background:#fff}th,td{padding:13px;text-align:left;border-bottom:1px solid var(--line)}th{background:var(--ink);color:#fff}.formats,.revenue{display:grid;grid-template-columns:repeat(2,1fr);gap:10px}.formats>div,.revenue>div{background:#fff;padding:20px}.formats b{color:var(--accent);font:24px Georgia}.formats h3,.revenue b{font-size:14px}.formats p,.revenue p{font-size:10px;line-height:1.7;color:#68736f}.revenue span{display:block;color:var(--accent);font-size:8px}.revenue button{border:0;background:#ddd;color:#777;padding:10px;width:100%;font-size:9px}.review{background:#e7e8e2;padding:24px}.review label{display:block;margin:10px;font-size:11px}.review p{font-size:9px}footer{margin-top:80px;background:var(--ink);color:#fff;padding:45px max(20px,calc((100vw - 1120px)/2))}footer p,footer small{font-size:9px;color:#95a39f}#ageGate{border:0;width:min(500px,90vw);padding:35px;text-align:center}#ageGate::backdrop{background:rgba(4,12,10,.92)}#ageGate strong{display:grid;place-items:center;margin:auto;width:70px;height:70px;border-radius:50%;background:#a52424;color:#fff;font-size:22px}#ageGate div{display:grid;grid-template-columns:1fr 1fr;gap:8px}#ageGate a,#ageGate button{padding:12px;border:0;text-decoration:none;background:#ddd;color:#14231f;font-size:10px}#ageGate button{background:#a52424;color:#fff}@media(max-width:760px){body>header{padding:0 14px}header nav{display:none}main{padding:0 16px}.hero{display:block;min-height:auto;padding:60px 0}.hero h1{font-size:37px}.hero aside{margin-top:35px}.strategy,.guides,.formats,.revenue{grid-template-columns:1fr}.article-title h1{font-size:31px}.table-wrap{overflow:auto}}`;
if(process.argv.includes('--check')){const errors=[];if(cfg.verticals.length!==19)errors.push(`verticals must be 19, got ${cfg.verticals.length}`);const topics=cfg.verticals.flatMap(v=>v.topics.map(t=>`${v.slug}:${t}`));if(new Set(topics).size!==topics.length)errors.push('duplicate vertical/topic');if(new Set(domainAssets.domains.map(x=>x.domain)).size!==domainAssets.domains.length)errors.push('duplicate registered domain');for(const a of domainAssets.domains){if(!cfg.verticals.some(v=>v.slug===a.vertical))errors.push(`${a.domain}: unknown vertical`);if(!domainAssets.pricingProfiles[a.profile])errors.push(`${a.domain}: pricing profile missing`)}for(const v of cfg.verticals){if(!domains[v.slug])errors.push(`${v.slug}: domain missing`);if(!v.topics.length)errors.push(`${v.slug}: no topics`)}if(errors.length){console.error(errors.join('\n'));process.exit(1)}console.log(`portfolio check passed: ${cfg.verticals.length} sites, ${topics.length} topic blueprints, ${domainAssets.domains.length} registered domains`);process.exit(0)}
await fs.rm(out,{recursive:true,force:true});await fs.mkdir(path.join(out,'assets'),{recursive:true});const write=async(rel,data)=>{const p=path.join(out,rel);await fs.mkdir(path.dirname(p),{recursive:true});await fs.writeFile(p,data)};await write('assets/portfolio.css',css);for(const v of cfg.verticals){await write(`${siteDir(v)}/index.html`,home(v));for(let i=0;i<v.topics.length;i++)await write(`${siteDir(v)}/guides/${pad(i+1)}/index.html`,guide(v,v.topics[i],i));await write(`${siteDir(v)}/robots.txt`,`User-agent: *\nAllow: /\n# Draft site: every HTML page currently carries noindex\n`);await write(`${siteDir(v)}/sitemap.xml`,`<?xml version="1.0"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${v.topics.map((_,i)=>`<url><loc>https://${domains[v.slug]}/guides/${pad(i+1)}/</loc></url>`).join('')}</urlset>`)}const cards=cfg.verticals.map(v=>`<a href="/${siteDir(v)}/"><b>${pad(v.rank)}</b><div><span>${esc(v.name)}</span><h2>${esc(v.brand)}</h2><p>${esc(v.reason)}</p><small>${esc(domains[v.slug])} · ${v.topics.length}ジャンル · ${registeredDomains.has(domains[v.slug])?'取得済み':'未取得'}</small></div></a>`).join('');await write('index.html',`<!doctype html><html lang="ja"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="robots" content="noindex"><title>SCARLET 19-Site Portfolio</title><style>${css}.portfolio{max-width:1120px;margin:auto;padding:70px 0}.portfolio>h1{font-size:48px}.portfolio>p{color:#68736f}.portfolio-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-top:40px}.portfolio-grid>a{display:grid;grid-template-columns:55px 1fr;background:#fff;padding:20px;text-decoration:none;color:#14231f}.portfolio-grid>a>b{font:32px Georgia;color:#e34b35}.portfolio-grid span{font-size:9px;color:#e34b35}.portfolio-grid h2{margin:4px 0;font-size:18px}.portfolio-grid p{font-size:10px;color:#68736f}.portfolio-grid small{font-size:8px}@media(max-width:700px){.portfolio{padding:40px 15px}.portfolio-grid{grid-template-columns:1fr}}</style></head><body><main class="portfolio"><p class="eyebrow">SCARLET MULTI-SITE SYSTEM</p><h1>19の専門サイトMVP</h1><p>${esc(cfg.strategy)}。主ドメイン18件・防衛ドメイン8件を取得済み。全ページはレビュー完了までnoindexです。</p><div class="portfolio-grid">${cards}</div></main></body></html>`);const manifest={generatedAt:new Date().toISOString(),domainAssets:{registered:domainAssets.domains.length,primaryRegistered:cfg.verticals.filter(v=>registeredDomains.has(domains[v.slug])).length,redirectDomains:domainAssets.domains.filter(x=>x.role==='redirect').length,firstYearCost:assetCost,currency:domainAssets.currency,missingPrimaryDomains:domainAssets.missingPrimaryDomains},sites:cfg.verticals.map(v=>({rank:v.rank,slug:v.slug,name:v.name,brand:v.brand,domainCandidate:domains[v.slug],registered:registeredDomains.has(domains[v.slug]),aliases:aliasesFor(v.slug),risk:v.risk,topics:v.topics.length,pages:v.topics.length+1,indexPolicy:cfg.defaults.indexPolicy}))};await write('portfolio-manifest.json',JSON.stringify(manifest,null,2));await write('domains.csv','rank,site,brand,domain\n'+cfg.verticals.map(v=>`${v.rank},${v.name},${v.brand},${domains[v.slug]}`).join('\n'));await write('domain-assets.csv','domain,vertical,role,status,first_year_total_jpy\n'+domainAssets.domains.map(x=>`${x.domain},${x.vertical},${x.role},${domainAssets.status},${domainAssets.pricingProfiles[x.profile].firstYearTotal}`).join('\n'));await write('redirect-plan.csv','source,target,status_code\n'+domainAssets.domains.filter(x=>x.role==='redirect').map(x=>`${x.domain},${domains[x.vertical]},301`).join('\n'));console.log(`generated portfolio: ${cfg.verticals.length} sites, ${manifest.sites.reduce((n,s)=>n+s.pages,0)} pages, ${domainAssets.domains.length} domains`);

