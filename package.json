
{
  "name": "abeshi-mvp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "build:site": "node scripts/generate-site.mjs",
    "serve:site": "vite generated-site --host 0.0.0.0",
    "test": "node --test && node scripts/generate-site.mjs --check"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}

